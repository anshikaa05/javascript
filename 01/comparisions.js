console.log("2"===2);
console.log(2==2);

// console.log(null>=0); null is converted to 0 so thus ut comes to be true
console.log(null<0);
console.log(null==0);

console.log(undefined == 0);
console.log(undefined>0)
console.log(undefined<0)

// Summary that we could take is difference between == and ===
== any type of type of datatype is verified
=== a bit strict only for simlar type of datatype comparisions could happen